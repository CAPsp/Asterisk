using UnityEngine;
using System.Collections;

public class NumStage : MonoBehaviour {

    public static int numberOfStage = 1;

	// Use this for initialization
	void Start () {
        DontDestroyOnLoad(this);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
